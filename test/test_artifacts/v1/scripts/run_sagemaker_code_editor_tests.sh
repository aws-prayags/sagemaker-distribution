#!/bin/bash

# Exit immediately if a command exits with a non-zero status.
set -e

sagemaker-code-editor --version
echo "Verified that sagemaker-code-editor is installed"

# check that version output is not null - command succeeds basically

# check that there are at least 3 extensions - confirm the exact ones

# check that settings file is persisted
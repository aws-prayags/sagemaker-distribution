# Run: conda env update --file environment.yml -n sagemaker-distribution
channels:
  - conda-forge
dependencies:
  - python=3.10
  - conda
  - docker-py
  - pip
  - boto3
  - pytest
  - pytest-mock
  - pytest-xdist
  - semver
